package xyz.volcanobay.cabalist.content.entropy.blocks;

import net.minecraft.world.level.block.Block;
import xyz.volcanobay.cabalist.content.entropy.networks.Entropetic;

public class OscilistoneBlock extends Block implements Entropetic {
    public OscilistoneBlock(Properties blockProperties) {
        super(blockProperties);
    }
}
